apply plugin: 'groovy'
apply plugin: 'maven'

group = 'il.co.galex.tools.build'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    repositories {
        mavenCentral()
    }

    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:1.5.0'
}

install {
    repositories.mavenInstaller {
        pom.artifactId = 'permissions'
    }
}